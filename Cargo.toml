[package]
name = "ext-sort"
version = "0.1.0"
edition = "2021"

[dependencies]
rmp-serde = "^0.15"
serde = { version = "^1.0", features = ["derive"] }
tempfile = "^3.2"

[dev-dependencies]
rstest = "^0.12"
